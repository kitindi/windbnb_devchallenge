import React from 'react'

const HotelCard = ({hotel}) => {
    return (
        <div className="card">
            <img src={hotel.photo} alt=""/>

            
        </div>
    )
}

export default HotelCard
